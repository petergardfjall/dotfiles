# make bootable usb
# find out device name of usb (e.g., /dev/sdh)
lsblk
# write image to usb
sudo dd if=xubuntu-16.04-desktop-amd64.iso of=<usb-device-path> bs=4M
# flush file system buffers
sudo sync
