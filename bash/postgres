#
# 'pg-local mydatabase' sets up environment varaiables for accessing a local
# development database with 'psql'.
#
function pg-local() {
	 export PGUSER=postgres PGPASSWORD=password PGHOST=localhost PGDATABASE=${1}
}

# equivalent to always running with "\psql pager off"
export PAGER=