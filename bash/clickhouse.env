#
# 'clickhouse-local [mydatabase]' accesses a local development database with 'clickhouse-client'.
#
function clickhouse-local() {
    clickhouse-client --user=clickhouse --password=password --host=localhost --port=9000 "$@"
}
